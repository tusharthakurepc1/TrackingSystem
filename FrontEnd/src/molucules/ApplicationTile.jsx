import React from 'react'
import {Calendar} from 'react-calendar'
import './style.scss'

const ApplicationTile = (props) => {
    let {date, email, orgName, status} = props.user
    return (
        <>
            
        </>
    )
}

export default ApplicationTile;